// Constants for CLI commands

/// Default fee amount
pub const DEFAULT_FEE: u64 = 1000;

/// Default gas limit
pub const DEFAULT_GAS_LIMIT: u64 = 1000000;

/// Default confirmation blocks
pub const DEFAULT_CONFIRMATIONS: u64 = 6;

/// Default network (testnet)
pub const DEFAULT_NETWORK: &str = "testnet";
